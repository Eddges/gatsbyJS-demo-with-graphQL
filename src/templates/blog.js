import React from 'react'
import Layout from '../components/Layout/Layout'

const Blog = (props) => {
    return(
        <div>
            <Layout>
                This is blog template
            </Layout>
        </div>
    )
}